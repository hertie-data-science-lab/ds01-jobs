# ds01-test-gpu-compute
Real GPU workload: matmul benchmark + mini training loop with PyTorch.
Tests: GPU allocation, CUDA access, real compute, result collection.
Expected: completes in ~1-2min (plus image pull), produces benchmark.json.
