# ds01-test-multi-file
Data analysis job producing multiple output types (CSV, PNG, JSON).
Tests: pip install in build, multiple result files, larger image build.
Expected: completes in ~1-2min, produces dataset.csv, analysis.png, summary.json.
